import { createStyle } from 'lib/utils'

export const skills = createStyle({
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'space-around',
    marginTop: '30px',
    padding: '20px'
})
